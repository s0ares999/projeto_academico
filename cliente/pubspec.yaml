name: pi4_academico
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  intl: ^0.18.0
  flutter_countdown_timer: ^4.0.0
  shared_preferences: ^2.0.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  http: ^1.2.2
  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/  # Certifique-se de que o arquivo do logo está no caminho correto
    - assets/images/logo_red.png
    - assets/images/logo_green.png